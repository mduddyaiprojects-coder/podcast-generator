{
  "name": "PostgreSQL Database",
  "type": "postgres",
  "data": {
    "host": "{{ $env.DB_POSTGRESDB_HOST || 'localhost' }}",
    "port": "{{ $env.DB_POSTGRESDB_PORT || 5432 }}",
    "database": "{{ $env.DB_POSTGRESDB_DATABASE || 'n8n' }}",
    "user": "{{ $env.DB_POSTGRESDB_USER || 'n8n' }}",
    "password": "{{ $env.DB_POSTGRESDB_PASSWORD }}"
  },
  "description": "PostgreSQL database for workflow data and content storage"
}
